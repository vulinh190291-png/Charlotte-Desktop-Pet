# 性能验收记录

日期：2026-09-13  
提交：`7fbcc9f92c163a71d02ee80cf27d0e834ee3f797`  
发布类型：win-x64 自包含目录发布  
素材：94 帧粗加工透明占位资产  
环境：Windows build 26200 / 25H2、x64、20 逻辑处理器、约 31.66 GiB 可用内存、当前显示器 150% DPI

原始数据：`artifacts/performance/performance-20260913-173716.json`（生成产物，不提交 Git）。

## 冷启动

新进程和独立测试资料目录各运行一次；没有清除操作系统文件缓存。

| 运行 | 首个诊断窗口句柄可见 |
| --- | ---: |
| 1 | 1236.83 ms |
| 2 | 763.06 ms |
| 3 | 750.07 ms |
| 4 | 715.84 ms |
| 5 | 654.92 ms |
| 中位数 | 750.07 ms |
| 最大值 | 1236.83 ms |

目标 `≤2s`：**Pass**。

## 无交互运行

预热 30 秒后采样 300.307 秒。角色按正常闲置规则播放；第 3 分钟进入 Rest，因此这里衡量的是“无用户交互运行”，不是强制锁定单一 Idle 动作。

| 指标 | 结果 | 目标 | 状态 |
| --- | ---: | ---: | --- |
| 平均 CPU | 0.3153% | <2% | Pass |
| Working Set 峰值 | 209.67 MiB | 记录项 | — |
| Private Memory 峰值 | 149.41 MiB | <250 MiB | Pass |

## 隐藏运行

通过只允许项目 `artifacts` 测试配置使用的 `--start-hidden` 启动；采样 60.848 秒。

| 指标 | 结果 |
| --- | ---: |
| 平均 CPU | 0.0385% |
| Working Set 峰值 | 134.40 MiB |
| Private Memory 峰值 | 76.73 MiB |

隐藏后的 CPU 和内存均明显低于可见无交互状态，符合停止逐帧渲染的预期。该开关只验证隐藏渲染状态；真实全屏应用触发及恢复仍由实机矩阵单独验收。

## 测量边界

- 进程带 `--diagnostic-shell`，以可观察窗口句柄定义“首帧窗口可见”；正式默认仍不显示任务栏按钮。
- 未清空 Windows 文件缓存，以上不是系统重启后的绝对冷缓存数据。
- 当前只有一台 Windows 11 机器和一个显示器，不能外推为 Win10 或混合 DPI 结果。
- 正式动画替换后应重新运行同一脚本，因为帧复杂度、压缩率和尺寸会影响解码及内存。

## 2026-09-14 自动 Walk 回归采样

完成自动 Walk 窗口接线后，以 2 次冷启动、2 秒预热、45 秒可见无交互和 10 秒隐藏做受影响范围短采样。45 秒窗口必然覆盖至少一次 20–40 秒抽样 Walk：冷启动最大 774.72ms，平均 CPU 0.2922%，Private Memory 峰值 149.05MiB；隐藏平均 CPU 0.1617%。三项发布阈值均通过。此短采样用于排除自动位移造成的明显性能回归，不替代上方 5 分钟完整记录。

## 2026-09-14 帧解码与特效层回归采样

完成 DPI 分版本解码、Alpha 掩码缓存、边界预约和独立特效窗口后，以 1 次冷启动、1 秒预热、15 秒可见无交互和 5 秒隐藏做快速回归。结果文件为 `artifacts/performance/performance-20260914-113335.json`：冷启动 732.12ms，平均 CPU 0.3694%，Private Memory 峰值 146.67MiB；隐藏平均 CPU 0.1541%、Private Memory 峰值 96.91MiB。三项发布阈值均通过；短采样不替代上方完整长测。
